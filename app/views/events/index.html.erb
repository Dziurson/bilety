<h1>Wszystkie wydarzenia</h1>

<table>
  <thead>
    <tr>
      <th>Artist</th>      
      <th>Data koncertu</th>      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td><%= link_to event.artist, "/events/#{event.id}" %></td>        
        <td><%= event.event_date %></td>        
        <th colspan="3"></th>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nowe wydarzenie', new_event_url %> 
